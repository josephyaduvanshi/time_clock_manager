import 'package:flutter/material.dart';

class TimeTacticAnalytics extends StatelessWidget {
  const TimeTacticAnalytics({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("TimeTacticAnalytics"),
    );
  }
}
